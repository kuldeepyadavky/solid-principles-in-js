// Logger function responsible for displaying general messages
export function logMessage(message) {
  console.log(message);
}

// Logger function responsible for displaying error messages
export function logError(error) {
  console.error(error);
}
